# lyric-stage — 計画

曲に合わせて文字を「刻む」字幕演出ステージ。AviUtl の字幕演出を Web で再現し、GitHub Pages で公開する。

- 参考: 字幕演出を刻む【AviUtl】 https://youtu.be/JSFsIsjWINk
- 楽曲: 【魔王魂公式】シャイニングスター https://youtu.be/Qd01-6xVSHk
- 公開先: https://bubbleshaker.github.io/lyric-stage/

## 技術選定

| 採用 | 理由 |
|---|---|
| **GSAP 3 + SplitText** | 2025/04 に全プラグインが無料化。SplitText がテキストを文字/単語/行の `<span>` に分解し、timeline で 1 文字ずつ時間差アニメができる。字幕演出そのものの用途 |
| **Vite + TypeScript** | ビルドと dev サーバー。型があると演出定義の JSON スキーマを間違えにくい |
| **Web Audio / `<audio>.currentTime`** | 演出のマスタークロックは音声の再生位置。`requestAnimationFrame` で毎フレーム参照し、GSAP timeline の再生位置を同期させる |
| Canvas 2D | 背景の星空のみ。文字は DOM に任せる |

不採用: p5.js（文字の分解・配置を自前実装することになる）、three.js（3D フォント変換が必要で過剰）、anime.js（timeline は GSAP が上）

## 素材の扱い

- 楽曲は魔王魂の「シャイニングスター」。[利用ルール](https://maou.audio/rule/) に従い **クレジット「音楽：魔王魂」をページと README に明記**する。
- mp3 は `public/audio/` に置く（Web サイトの構成要素としての設置）。曲単品の再配布は規約で NG のため、README に「素材配布を目的としたものではない」旨を書き、加工版は置かない。
- **mp3 の取得はブラウザから手動で行う。** 魔王魂のサーバーはスクリプトからの直接ダウンロードを 403 で拒否する設定になっており、これは意図的な保護なので迂回しない。手順は README の「作者向け: 音源を入れ直すとき」。ファイルが無ければ公開サイトは「音源が見つかりません」と表示する。
- **音源と歌詞は作品の一部としてリポジトリに含める**（2026-08-19 に方針変更）。鑑賞者が素材を選ぶ UI は作らず、1 つの作品として固定する。`?lyrics=` は制作中の切り替え用に残し、公開時の既定は本編のシートにする。
- 歌詞テキストは公式の歌詞ページから作者自身がコピーして `public/lyrics/shining-star.json` に貼る。
- **各行の `time` は公式 MV の焼き込み字幕を基準にする**（2026-08-20 に投入）。MV 本体は取得できないので、storyboard（1.986 秒ごとのサムネイル 144 枚）から字幕の切り替わりフレームを読み、約 2 秒幅に絞った区間を mp3 の拍の格子（79.85 BPM / 1 小節 3.0055 秒）に載せて確定させた。MV は 2 行を 1 枚の字幕にまとめるので、2 行組の 2 行目は組の中央を拍に丸めて置いている。M6 のタイミング入力ツールができたら、この値を出発点に詰め直す。
- **未確認**: 魔王魂の利用ルールが明文化しているのは主に音源の利用条件で、歌詞テキストの Web 掲載可否は読み取れない。公開前に確認する。
- `.gitignore` は許可リスト方式。作品に使う素材だけを明示的に追跡し、作業中の未加工音源を履歴に焼き込む事故を防ぐ。

## マイルストーン

- [x] **M0** Vite 雛形 + GitHub Actions で Pages へデプロイ。空のステージが表示される
- [x] **M1** 音源の再生 UI（再生/停止・シーク・クレジット表示）
- [x] **M2** タイムライン基盤。`lyrics.json`（time, text, effect）を読み、`audio.currentTime` に同期して行を出し入れする
- [x] **素材投入** mp3 と本編の歌詞シートをコミットし、既定シートを本編に切り替える
- [x] **M3** 演出プリセット第一弾（fade / typewriter / bounce）を GSAP SplitText で実装
- [x] **M4** 演出プリセット第二弾（グリッチ・ズームイン・縦書き・文字分裂など、参考動画の「刻む」系）
  - [x] **M4-1** 今の型のまま書ける「刻む」系（glitch / zoom / shatter）
  - [x] **M4-2** `Effect` が行の要素も受け取れるよう型を広げ、縦書きを足す
  - [x] **M4-3** 本編シートへの演出割り当て
  - [x] **M4-4** `prefers-reduced-motion` への対応（M4-1 のレビュー指摘）
- [x] **M5** 背景演出（星空 Canvas、曲の盛り上がりに反応）
  - [x] **M5-1** 星空 Canvas の基盤（曲の再生位置に合わせて星が瞬く）
  - [x] **M5-2** 曲の盛り上がりへの反応（実音を AnalyserNode で解析する）
- [ ] **M6** タイミング入力ツール。再生しながらキーを叩くと行の time が記録され、JSON として書き出せる
  - [x] **M6-1** タイミング記録の純粋ロジック（`domain/tap-session.ts`）
  - [x] **M6-2a** 収録ページ `tap.html`（開発用。キー入力・進捗表示・JSON 書き出し）
  - [x] **M6-2b** 収録途中の下書きの保存と再開
  - [ ] **M6-3** ツールで本編シートの time を実測に置き換える（[Issue #31](https://github.com/bubbleShaker/lyric-stage/issues/31) に段取りを書いた。**本体は収録作業でコードはほぼ無い**）
- [ ] **M8** 文字PV 化（2026-08-22 に方針転換。参考: 【Aviutl】文字PV - ロウワー https://youtu.be/3ioQDnOknuY ）
  - [x] **M8-0** 作品の尺をラスサビ 1 ブロック（約 27 秒）に絞る（[Issue #32](https://github.com/bubbleShaker/lyric-stage/issues/32)）
  - [ ] **M8-1** 行ごとの構図（画面のどこに・どのくらいの大きさで・どの傾きで）をシートに持たせる
  - [ ] **M8-2** 配色と背景の差し替え（星空 → 文字PV 風）
  - [ ] **M8-3** 図形グラフィック（帯・枠・グリッド・英字サブテキスト）
  - [ ] **M8-4** ビート同期の衝撃（フラッシュ・画面揺れ）。M5-2 の音量解析を流用する
- [ ] **M7** 仕上げ（レスポンシブ、OGP、README）。**M8 の後に回す**

各マイルストーンごとに Issue → ブランチ → reviewer レビュー → PR → マージ。

## レイヤー分け（M2 の入り口で引く線）

演出が増えても崩れないように、依存の向きを先に決めておく。

- `domain/` — GSAP も DOM も import しない。`LyricLine` 型、JSON の検証、「`currentTime` から今表示すべき行を決める」純粋関数。外側に求める口（`Playback` / `LyricPresenter`）も `domain/ports.ts` に置く。**依存の矢印を全て内向きに揃えるため**
- `app/` — `Ticker`（rAF はアプリ全体で 1 本）、歌詞ロード、domain の判定結果を presenter に渡す配線
- `stage/` — GSAP + SplitText による描画、`<audio>` のラッパ。GSAP に依存してよいのはここだけ。port の実装を提供する
- `main.ts` — 各層を組み立てて起動するだけ（文言もアニメーション定義も持たない）

決めたこと:
- **クレジット表記は HTML を正とする。** 歌詞 JSON には `credit` を持たせない（表示位置が固定で、シートごとに変える必要が無いため）
- 作品固有の値（音源パス・既定の歌詞シート名）は `src/work.ts` に集める。`app/` は「`?lyrics=` を読む」「`public/lyrics/<name>.json` を取る」という曲に依らない仕組みだけを持つ。複数曲に対応する時はここを JSON へ移す

演出プリセットは `switch (effect)` で分岐せず、`(chars: Element[]) => gsap.core.Timeline` を値に持つレジストリ（`effects` オブジェクト）にする。**新しい演出＝レジストリに 1 エントリ足すだけ**にするため。表示側（`LyricStage`）は演出が増えても変わらない。

M3 で決めたこと:
- **文字送りの合計には上限（0.8 秒）を設ける。** 本編の行間隔は最短 2.25 秒・最長の行は 28 文字あり、1 文字ごとの遅延を固定にすると出揃う前に次の行へ切り替わる。`staggerFor(文字数, 希望の間隔)` が短い行では希望どおり、長い行だけ詰める
- **`Effect` の型は `(chars) => Timeline` のまま据え置く。** 行全体や単語単位が要るのは M4 の「刻む」系。必要になった時点で広げる
- **退場演出は持たない。** 行の切り替えは今のとおり即座に消える。入れると `clear()` が非同期になり `LyricStage` のライフサイクル管理が一段複雑になるので、演出が出揃ってから別途考える
- **本編シートへの演出の割り当ては M4 の後。** 先に振ると「刻む」系が出揃った時にやり直しになる
- 演出だけを音源抜きで見る `effect-preview.html`（開発用、Vite の入口は index.html だけなので dist に入らない）

M4-1 で決めたこと:
- **型を広げるのは縦書きが要るまで先送りする。** glitch / zoom / shatter は文字の位置・回転・不透明度だけで書けるので `(chars) => Timeline` のまま足せる。縦書きだけは行の要素に `writing-mode` を当てる必要があり、それが型を広げる唯一の理由。使う当てのない引数を先に増やさない
- **`zoom` は文字ごとに拡大する（行全体ではない）。** 行の要素を触れないため。行全体が迫ってくる本来のズームは M4-2 で root を受け取れるようになってから別名で足す
- **gsap は影のような複合文字列の中の「負のゼロ」（`-0em`）を読み違える**（桁が変わり左右非対称になる）。glitch の RGB ずれは終点を `0.001em` にして避けている。目視では effect-preview.html で `gsap.globalTimeline.pause()` → `.time(秒)` のコマ送りが要る（演出は 1 秒未満で終わるため）
- 「文字数が増えても総時間が伸びない」検査は、**30 文字で文字送りの上限に達することに暗黙に依存していた**（既存 3 つは希望間隔が広く、たまたま成立していた）。間隔の狭い演出を足すと作りは正しいのに落ちるので、「総時間 ≤ トゥイーン単体の長さ + `MAX_STAGGER_SPAN`」という本来の不変条件に書き換えた。ただし上限を「超えない」だけだと間隔を極端に狭く書いた演出が素通りするので、「文字数を 4 倍にしても総時間が変わらない＝本当に頭打ちになっている」ことも併せて見る
- **飛距離・ずれ幅は px でなく `xPercent` / `yPercent`（文字自身の大きさに対する割合）で書く。** 文字サイズが `clamp(1.75rem, 7vw, 5rem)` で 28px〜80px まで変わるため、px 固定だと狭い画面で飛びすぎ、広い画面で控えめになる
- **`prefers-reduced-motion` は未対応**（M4-4 に起こした）。zoom・shatter・glitch は前庭系の症状を誘発しうる部類。`effects` レジストリは純粋なまま保ちたいので、stage 層で `gsap.matchMedia()` を使って fade に落とす形にする

M4-2 で決めたこと:
- **`Effect` の引数はオブジェクト 1 個（`EffectTarget = { root, chars }`）にする。** `(chars, root)` と位置引数を並べる形は採らない。SplitText は `words` / `lines` も作れるので、後で足すときに**それを使わない既存の演出を書き換えずに済む**ため
- **演出は DOM を触らない。レイアウトは宣言する。** `writing-mode` は補間できるプロパティではなくレイアウトの指定なので gsap では動かせない。かといって演出自身が `classList.add` すると、**付けるのは演出・外すのは stage** という非対称ができ、規約でしか守れない不変条件が生まれる。そこでレジストリのエントリに `{ layout, build }` の形も書けるようにし、**当てるのも外すのも `LyricStage`** に寄せた
  - レジストリは `Effect | EffectDef` の union。レイアウトを要求しない 7 つは関数のまま書ける。全エントリを `{ build: ... }` で包むと 1 つの例外のために残りが読みにくくなるため
  - 副産物として、**レイアウトの決定が SplitText より前**になった。分割は組み方が決まった後に行われる必要がある（`type: 'lines'` を足したとき、横組みで測った行区切りで縦組みを表示することになる）
  - もう 1 つの副産物として、テストのダミー root がただの `{}` で済むようになった
- 縦組みは**高さがインライン方向**になるので、`max-height` を絞ると 1 段に収まらないぶんが右から左へ段になる
- **画面下の `.transport`（約 105px）は `.stage` の `padding-bottom` で一括して避ける。** 縦書きの `max-height` を削って避けようとすると、画面が低いとき（横持ちのスマホ）に 1 段 2 文字の段だらけになって縦書きとして読めなくなる。**場所を空ける**方が正しい。代償として歌詞の中心は画面中央よりやや上になる
- **`max-height: 100%` は grid の中では効かない。** 百分率の解決先が定まらず `max-height` ごと無効になり高さが青天井になる（実測）。`calc(100dvh - 10rem)` と書く
- **縦組みの文字サイズは高さにも追従させる**（`clamp(1.5rem, min(7vw, 8dvh), 5rem)`）。横組みは幅だけで決めてよいが、縦組みは高さが行の伸びる向きなので、幅だけで決めると低い画面で段だらけになる
- `zoomLine`（行全体が迫る）は `chars` を一切使わない。型を広げた意味がそのまま出ている。今の `zoom` は文字ごとの拡大なので別物として残す
- **`zoomLine` の再生中は行が画面より大きくなり、再生コントロールに重なる。** 手前から迫る演出の性質そのものなので直さない
- ~~**未決**: 本編には `I'll believe of my sensation` のような Latin の行がある~~ → M4-3 で決着（縦書きは日本語の行にだけ当てる）

M4-3 で決めたこと:
- **1 番・2 番・ラスサビの対応する行には同じ演出を当てる。** 同じ歌詞に毎回別の演出を振ると画は賑やかになるが曲の構成が散る。繰り返しを「型」にすると、`シャイニングスター綴れば` が来る＝サビ頭、`I'll believe of my sensation` が来る＝サビの折り返し、と画だけで分かる。`src/lyric-sheets.test.ts` の「同じ歌詞の行には同じ演出が当たっている」がこの方針を守る（方針を変えるときは検査ごと書き換える）
- **本編シートは全行に `effect` を明示する。** 省略しても既定の fade で動いてしまうので、「意図して fade を選んだ行」と「割り当てを忘れた行」が画面を見ても区別できない。検査で書き忘れを落とす。sample.json は既定値の挙動を見せる役なので対象外
- **Latin の行は縦書きにしない**（M4-2 の未決の決着）。`text-orientation` を指定しない `vertical-rl` は Latin を横倒しにする。これは日本語縦組みの慣例どおりの正しい挙動なので、CSS で場合分けして戦うより、**縦書きを日本語の行にだけ当てる**方が素直。`text-orientation` は足さない。「縦書きの行にラテン文字が含まれていない」検査で守る
- **`vertical` は 6 秒前後の伸ばしの行に当てる**（`さざなみの音に癒やされてく` / `月の光 隠す雲は揺らぎ` / `切なさやトキメキが輝き出す`）。縦組みは画が大きく変わるぶん場面転換として効くが、行が速く入れ替わる区間で使うと組み方が毎行変わって落ち着かない。いずれも間奏やサビ前の余白にあたる
- **最後の行にも `duration` を書く**（レビュー指摘）。最終行だけは「次の行の `time`」が無いので、書かないとアウトロの約 41 秒を出しっぱなしになる。既存の検査も最終行の猶予は `Infinity` として扱うのですり抜けていた。アウトロで実際に `LaLaLa` がどこまで歌われているかは未確認なので、他の伸ばしの行と同じ 6 秒を暫定値として置いた（M6 で詰める）
- 割り当ての骨格: A メロ前半 `fade` → 後半 `typewriter`（漂う情景から言い聞かせる語りへ）、決意と余韻の行は `bounce`（`悩むのはもうやめた` / `大空を翔る` / `トキメキを感じて` / `熱いメロディになる` / `LaLaLa`）、サビ頭は `zoomLine`、`降り注ぐ` / `イマジネーション` は `shatter`、短い決めの行は `zoom`、Latin の行は `glitch`。`typewriter` は A メロ後半のほかサビ内の `魔法が使えるような` にも当てている（呪文を唱える感じが合うため）
- **歌詞が違う行では、構成上の対応より歌詞の意味を優先する。** 1 番と 2 番のサビ前は構造的には同じ位置だが、`時を閉じ込めて` は `typewriter`（1 文字ずつ閉じ込める）、`大空を翔る` は `bounce`（上昇）と分かれている。「対応する行は同じ演出」は**同じ歌詞**についての方針で、位置についての方針ではない

M4-4 で決めたこと:
- **`gsap.matchMedia()` は使わない**（M4-1 の申し送りからの変更）。あれの価値は「クエリが変わった瞬間に走行中のトゥイーンを revert する」ことで、そのために context を張る。今回の演出は 1 秒未満で行ごとに作り直すので、**組み立てる時に 1 回クエリを読めば同じ結果**になる。context を持ち込まないぶん `LyricStage` も gsap の文脈抜きで組み立てられる
- **責務を 3 つに割る。** `resolveEffect(name, reducedMotion)` が「どの演出に落とすか」、`LyricStage` が「いつ読むか」（行を出すたび。曲の途中で設定を変えても次の行から効く）、`main.ts` が「どう読むか」（`window.matchMedia`）。`effects.ts` は `window` に触らないままなので、この判断はブラウザ無しでテストできる
- **落とし先は `fade` ではなく `calm`（新設）。** `fade` は控えめとはいえ `yPercent: 40` の移動があり、動きを減らす設定への答えとしては半端。`calm` は行ごと不透明度だけ変える 1 本のトゥイーン。シートから直接指定してもよい（静かに出したい行のため）
- **`layout` は落とさない。** `writing-mode` は動きではなくレイアウトの指定なので、動きを減らす設定でも縦書きは縦書きのまま読めるべき。ここを一緒に落とすと、縦書き前提で決めた文字サイズや段組み（`style.css`）ごと外れる。`resolveEffect` が `{ layout, build }` を返す形にしてあるので `build` だけ差し替えられる
- **`LyricStage` の第 2 引数に既定値を置かない**（レビュー指摘）。最初は「減らさない」を既定にしていたが、それだと新しい所で組み立てた時に渡し忘れてもエラーにならず、**アクセシビリティの機能が静かに無効になる**。読み方は `src/lib/reduced-motion.ts` に `systemReducedMotion()`（OS 設定）と `neverReduceMotion`（常に減らさない）の 2 つを置き、本編と `effect-preview.html` がそれぞれ明示的に渡す。開発用ページで素の演出が出るのは「減らさないという選択」であって既定値の副作用ではない、という形にした
- **未知の演出名の警告には実際の落とし先を書く**（レビュー指摘）。動きを減らす設定では `calm` が使われるのに「既定の fade を使います」と出ると、綴りの間違いを調べている人を誤導する

M5-1 で決めたこと:
- **背景の時計も `player.currentTime`。** 実時間（`performance.now()`）で回すとシークで背景だけ置いていかれる。「マスタークロックは音声の再生位置」に揃えると、停止中は背景も静止し、シークすれば瞬きの位相も一緒に飛ぶ
- **`ReducedMotionQuery` は背景にも必須引数で渡す**（M4-4 のレビュー指摘）。文字と背景で同じ query を `main.ts` から配る。**落とし先は「時刻 0 の星空」**。星を消さないのは `calm` を新設した時と同じ判断で、背景ごと消すと作品が別物になるため。動かない点でも星空は星空
  - 時刻を 0 に畳んだ副産物として、**2 フレーム目以降は前回と入力が同じになり描画そのものが飛ぶ**。動きを減らしたい人の端末で同じ絵を 60 回/秒 描き続けずに済む
  - **CSS の `@media (prefers-reduced-motion: reduce)` は足さない**（M4-4 の申し送りへの回答）。Canvas の動きは JS 側でこの query を読んで決めるので、CSS 側に持たせると同じ判断が 2 か所になる
- **星の配置は種を固定した擬似乱数**（`lib/random.ts` の mulberry32）。リロードのたびに星空が変わらない＝配置も作品の一部として固定される。副産物としてテストで検査できる。星の数は画面の広さに依らず固定で、リサイズしても空は作り替わらない（位置は px でなく 0〜1 の割合で持つ）
- **描画面の辻褄合わせは `ScaledCanvas` に分ける**（レビュー指摘）。canvas は width / height 属性が実ピクセル数で CSS の大きさとは別物、という面倒はここに閉じ込め、`Starfield` は「星をどう作るか」と「いつ描くか」だけを持つ。表示倍率の読み方も注入する形（`systemPixelRatio`）にしたので、グローバルの差し替え無しで検査できる
- **`ResizeObserver` の初回通知は非同期**（購読した次の描画更新、しかも rAF より後）。大きさが決まるまで描かないと、最初の 1 フレームだけ既定サイズ（300×150）の座標系で星が左上に固まって描かれる。テストの偽物が `observe()` で同期に鳴らしていて、実ブラウザでは起こり得ない状態を前提にした検査になっていた
- **背景の失敗は本編に波及させない**（レビュー指摘）。canvas を塞ぐブラウザなどで context を作れないとき、`main.ts` のトップレベルで throw すると以降の組み立てが丸ごと止まり、真っ黒な画面に死んだ再生コントロールだけが残る。星が出ないことより悪い。組み立てを try で囲んで警告に留める
- **描き直すかどうかは描画の入力すべてで決める。** 今の入力は「時刻」と「描画面の版」。M5-2 で盛り上がりの強さが加わったら判定にも足すこと（足し忘れると、時刻が同じフレームで強さの変化が黙って捨てられる）
- **盛り上がりの口は M5-2 まで作らない**（Issue #20 で「先に置く」と書いたが撤回）。M4-1 で決めた「使う当てのない引数を先に増やさない」と揃える。常に 0 を返す実装を配線しても、検査できることが増えない
- 毎フレームの `render` では `clientWidth` を読まない。同じフレームで gsap が文字のスタイルを書き換えるので、読むとそのたびにレイアウトの再計算を強制することになる。大きさは `ResizeObserver` が控えた値を使う

M5-2 で決めたこと:
- **盛り上がりは実音から取る**（歌詞シートの構造から出す案・シートに区間を書く案は不採用）。狙いにまっすぐで、M2 で確定したシートの形を変えずに済み、歌詞の無い間奏でも背景が動く
- **見るのは低音域（〜200Hz）だけ。** 全帯域の音量だと歌とシンバルで常時飽和して、盛り上がりの差が出ない。キックとベースが曲の勢いを素直に表す。何 Hz までを見るかは `sampleRate / fftSize` からビン本数に逆算する（本数で書くと解析の細かさを変えた時に帯域が変わる）
- **`<audio>` 要素は composition root が持つ。** 再生の制御（`AudioPlayer`）と解析（`loudness.ts`）が同じ音を別々の側面から使う。`AudioPlayer` に解析まで持たせると「音を鳴らす」以上のことを知り始めるし、要素を晒す getter を足すと何でもできてしまう
- **`createMediaElementSource` に繋いだら `destination` まで繋ぐ。** 繋いだ時点で音の出口が AudioContext 側へ移るので、繋ぎ忘れると音が消える。**解析は動くので背景は正しく反応し、原因が分かりにくい。** 目でも耳でも気づけないので、`AudioContextFactory` を注入して**配線そのものをテストで守る**。同じ理由で、投げうる操作（`createAnalyser` / `fftSize`）は付け替えより前に済ませる（レビュー指摘）
- **`AudioContext` は再生ボタンのクリックを起点に起こす。** 音の自動再生と同じ制限で、操作なしには動き出せない。一度失敗したら諦める（押すたびに作ると数個で `new AudioContext()` 自体が投げるようになる／レビュー指摘）
- **窓（`quiet` / `loud`）は曲ごとの実測値なので `work.ts` が持つ**（レビュー指摘）。本編の mp3 は低音がほぼ鳴りっぱなしで、生の値はイントロ 0.73 / A メロ・サビ 0.91 / ラスサビ 0.95 と高い側に密集する。0〜1 をそのまま使うと常に振り切るので、中央値のあたりを 0 に置いて上だけを見る。テストは幅そのものではなく「単調・クランプ」という曲に依らない性質だけを検査する
- **平滑化はアタック速く・リリース遅く。** 生の値は毎フレーム暴れるので星が痙攣する。**無音では厳密に 0 まで落とす**（レビュー指摘）— 指数の減衰は 0 に漸近するだけなので、そのままだと停止中も「前回と違う強さ」が何分も続き、M5-1 の「同じ入力なら描かない」が効かなくなる
- **反映先は明るさと半径だけ。位置は動かさない。** 星が流れると歌詞から目が離れる
- **動きを減らす設定では強さも 0 に畳む。** 音での明滅も「動き」。時刻と同じ扱いにすることで「2 フレーム目以降は描かない」もそのまま効く
- `IntensityQuery` は `lib/intensity.ts` に置く（`ReducedMotionQuery` と対称）。**描くだけの `Starfield` が音声解析のモジュールを名指ししないため**

M6-1 で決めたこと:
- **収録は既存シートの上書きとして行う。** 歌詞を打ち直す形にはしない。`text` と `effect` は元シートから持ち出し、`time` と `duration` だけを叩いた値で置き換える。M4-3 で決めた演出の割り当てを収録のたびに作り直さずに済む
- **開始と終了の両方を叩ける。** 開始が `time`、終了が `duration`（= 終了 − 開始）。**終了を叩かなかった行は `duration` を持たない**（次の行まで表示）。M2 で決めたデータ構造のまま「間」を作れる。M4-3 で暫定 6 秒と置いた最終行もここで実測に置き換える
- **叩いた行の `duration` は終了の打鍵からしか作らない。** 元の `duration` を残すと、開始だけ録り直した行が「新しい開始 + 古い長さ」という、どちらの収録にも属さない値になる。長さを保ちたければ終了も叩く
- **人間の反応の遅れ（100〜200ms）は補正しない。** オフセット欄を作ると「叩いた値」と「書き出す値」が分かれ、どちらを見て詰めているのか分からなくなる。ズレは書き出した JSON を見ながら手で詰める
- **`undo` に履歴は持たせない。** 直前に叩いた行に終了が記録されていれば終了を、無ければ行ごと取り消す。打鍵は必ず「開始 → 終了」の順にしかならないので、この導出で直前の操作が分かる
- **ただし「直前に叩いた行」はカーソルから導出せず、状態に持つ**（`pending` / レビュー指摘）。当初は `cursor - 1` で代用していたが、任意の行へ飛べる `moveCursorTo` があると成り立たない。飛んだ先で叩いた終了が、**以前に録った別の行の `duration` を黙って書き換えてしまう**。`pending` を持てば「飛んだ直後は終了も取り消しも働かない（まず開始を叩く）」を素直に書ける
- **収録の区切りも状態に持つ**（`runStart` / 再レビュー指摘）。`pending` だけでは足りなかった。行ごと取り消したときに次の取り消し先を決める処理が `cursor - 1` と同じ導出に戻っており、**飛んだ地点を跨いで前回の収録成果を消したり、その行の `duration` を後から書き換えたり**できていた。「取り消しは今の収録の始まりより前へは遡らない」と決めれば、両方が同じ 1 つの規則で塞がる（`runStart = 0` の通常の収録では今までどおり最初まで遡れる）
- **書き出したシートが `time` の昇順であることを保証する**（レビュー指摘）。`LyricSheet` は「昇順に整列済み」を名乗る型で、`activeLineIndexAt` は二分探索でそれに依存している。収録の途中は**録った時刻と元シートの時刻が混ざる**ので、破れる経路が 2 つある
  - 逆行・同時刻の打鍵 → `tapIn` は、前に録った時刻（`end ?? time`）より後でなければ記録しない。**比較は丸めた後の値で行う**（書き出されるのはそちらなので、10ms 以内の連打も「同時刻」として弾ける）
  - 録った時刻が、まだ元の時刻のままの後ろの行を追い越す → `orderProblems` が衝突した行を返し、`toSheet` は衝突が残っていたら書き出さない。**並べ替えて直すことはできない。** 歌詞の順は作者が決めたもので、`parseLyricSheet` の整列（`time` 順）に任せると歌詞の並びそのものが変わる。「どの行から録り直せばよいか」を画面に出せる形で返す
  - 衝突は**録った時刻と、まだ元のままの推定時刻の間にしか起きない**ので、指された行まで叩き進めれば必ず解消する。書き出せない場面はあっても、収録の成果が消えるわけではない
  - **食い込み（`overlap`）の判定は足し算の結果を丸めてから比べる**（再レビュー指摘）。10ms の格子に載った値どうしでも和には誤差が出る（`10 + 1.12 = 11.120000000000001`）ので、丸めずに比べると**前の行の終了と次の行の開始がぴったり同じという正しい並びを食い込みと読み違え、書き出せなくなる**
  - **例外は `OrderConflictError` で `problems` を持つ**（再レビュー指摘）。「2 行目「に」が…」という言い回しは表示側の関心で、domain がそこまで決めると録り直す場所を知るのにメッセージを読み解く羽目になる（テストも文面に結び付く）
- **秒数は 10ms に丸めて書き出す。** 聴いて分かる差ではなく、JSON も読みやすい。丸めた結果 0 になるほど短い区間は間として書き出さない（`duration: 0` は不正なデータ）。**`duration` は丸めた後の時刻どうしで引き、引き算の結果も丸め直す**（`time + duration` が書き出した終了と一致し、`6.0000000000000036` のような値も JSON に出ない）
- **状態は書き換えず新しい値を返す。** 取り消しが「1 つ前の値に戻す」ではなく「1 つ前の操作を打ち消す」で書ける。無視される打鍵（録り終わった後・逆行・開始より前の終了）は同じ参照を返すので、画面側は差分を見るだけで描き直しを決められる
- **書き出す行は必ず複製する**（レビュー指摘）。未収録の行を元シートのまま返すと、返り値を書き換えた側が元シートまで変えてしまい、「状態は書き換えない」に穴が空く

M6-2 への申し送り:
- 読み込んだ JSON は**必ず `parseLyricSheet` を通してから** `startSession` に渡す。`startSession` は `LyricSheet` 型を無検証で受ける（検証は domain の入口である `parseLyricSheet` の仕事で、二重には持たない）
- 書き出しボタンは `orderProblems` が空でない間は押させない。`toSheet` の例外は最後の砦であって、画面に出す知らせはその手前で出す
- **衝突の知らせには前後どちらの行も出す。** `OrderProblem.index` は常に後ろ側の行を指すが、原因は前の行の打ち間違いであることも多い（例: 1 行目を 25 秒に録ってしまった）
- **収録途中の状態を下書きとして保存・再開できるようにする**（再レビュー指摘）。`toSheet` が衝突時に書き出さない設計で唯一失われるのは「リロードすると収録の途中が消える」ことなので、下書きの保存で塞ぐ。`toSheet` に「壊れていても出す」抜け道を作るよりよい
  - **復元は `resumeSession(source, takes)` を domain に足してから行う。`TapSession` を UI がオブジェクトリテラルで手組みしない**（再々レビュー指摘）。`TapSession` は `pending` と `runStart` を含み、`runStart <= pending < cursor` と `[runStart, cursor)` に穴が無いことを守っている。手組みを許すと、そこで `pending: cursor - 1` と書いた瞬間に**M6-1 で二度直した取り違えが UI 側で再発する**。保存するのは `takes` だけでよく、`cursor` は最後の take の次、`pending` は無し、`runStart` は `cursor` から決まる
- **衝突している行へ飛ぶ導線を置く。** `undo` は今の収録の始まりより前へ遡らないので、前の収録で作った衝突は `moveCursorTo` で戻って録り直すしか解消手段が無い。導線が無いと詰まったように見える
- 「最後の行を録り直す」導線に注意。`moveCursorTo(session, lines.length)`（録り終わりの位置）へ移すと取り消し先が無くなる。録り直したい行そのものへ移すこと

M6-2a で決めたこと:
- **道具の中身は `src/tools/` に置き、`tap.html` は薄い入口にする。** `effect-preview.html` はページに直書きしたので `tsconfig` の `include`（`src`）から外れ、型検査もテストも効かない（`LyricStage` のシグネチャを変えるたび手で直す注意書きが付いている）。収録ページは行の一覧・キー・書き出しと持ち物が多いので、同じ作りにはしない
  - `src/tools/` は「開発用の道具」の層。`app/` `stage/` `domain/` に依存してよいが、**そこから依存されてはいけない**。Vite のビルド入口は `index.html` だけなので、道具は本編のバンドルに混ざらない（`dist` に `tap.html` は入らない）
- **キーは単キー**（`Space` 開始 / `Enter` 終了 / `Backspace` 取り消し / 行をクリックで録り直し）。曲は待ってくれないので修飾キーとの組み合わせは使わない。**文字を打つ場所に焦点があるときだけ横取りしない** — シークバー（`type="range"`）は横取りしてよい。一律に除外すると、シークした直後に収録できなくなる
- **描き直すかどうかは、セッションの参照が変わったかで決める。** 無視された打鍵（録り終わった後・逆行・開始より前の終了）は同じ値が返るので、そのまま「描かない」に繋がる。M6-1 で不変にした利点がここで出る
- **書き出しはクリップボードと textarea の両方。** クリップボードは安全な文脈でないと拒否されるので、失敗しても textarea が残る形にする（収録をやり直さずに済む）
- **書き出した後に録り直したら、textarea の中身は消す**（レビュー指摘）。残すと、**見た目は正しい JSON なので気づけないまま実測と違う値を歌詞シートに貼る**。この道具の成果物そのものが壊れる経路なので、古くなった時点で消す
- **書き出しで焦点を移さない**（レビュー指摘）。`textarea` に焦点を移すと `Space` も `Backspace` も横取りされ、収録が続けられなくなる（`readonly` なので画面上は無反応に見え、道具が壊れたようにしか見えない）
- **書き出す文字列を作るのは純粋側（`tap-view.ts` の `exportText`）**（レビュー指摘）。この道具の成果物そのものなので、「書き出したものが歌詞シートとして読み直せる」を検査で守る。DOM の中に `JSON.stringify` を埋めるとそこだけ手動確認になる
- **押しっぱなしの自動リピート（`event.repeat`）は弾く**（レビュー指摘）。再生中は `currentTime` が進み続けるので、弾かないと**逆行のガードを素通りして数十行がまとめて焼かれる**（`Backspace` の長押しでは取り消しが暴走する）
- **無視された打鍵には理由を出す**（レビュー指摘）。「同じ参照が返ったら描き直さない」は実装としては綺麗だが、裏返すと逆行や録り終わりが**完全な無反応**になり、収録中に原因が分からない
- **`P` に再生・停止を割り当てる**（レビュー指摘）。`Space` を収録に使うため、焦点のある再生ボタンを `Space` で押す道が塞がる。これが無いとこのページの再生がマウス専用になる
- **`previewLines` を domain から公開する。** 衝突が残っている間 `toSheet` は投げるが、画面は「書き出したらこうなる」を出し続ける必要がある。**衝突しているときこそ**その値を見せないと、どこを録り直せばよいか分からない
- 再生・シークは本編と同じ `AudioPlayer` / `mountTransport` を使う。収録のためだけの再生系を別に持つと本編と挙動がずれる
- `requiredElement` を `lib/dom.ts` に出して `main.ts` と共用する（composition root が 2 つになったため）

M6-2b で決めたこと:
- **下書きに持たせるのは `takes` だけ。**`cursor` / `pending` / `runStart` は `resumeSession` が導出する。M6-1 の再々レビュー指摘どおり、`TapSession` を画面側がオブジェクトリテラルで手組みしない形にした（手組みを許すと `pending: cursor - 1` の取り違えが UI で再発する）
- **再開直後の `runStart` は `cursor`。** 取り消しは復元した分へ遡らない。`moveCursorTo` で飛んだときと同じ扱いで、**前の収録の成果を Backspace の連打で消せない**。副産物として `[runStart, cursor)` が空区間になるので、下書きの `takes` が飛び飛びでも不変条件は自明に保たれる
- **検証するのは形だけ。時刻の逆行は弾かない。** 弾くと収録の成果がまるごと失われるが、通せば既存の `orderProblems` が衝突として拾い、画面から録り直せる。**下書きを守る方を優先する**（`toSheet` に抜け道を作らない代わりに置いた仕組みなので、ここで捨てては本末転倒）
- **検証は domain（`resumeSession`）が持つ。** 置き場所（localStorage）は**手で書き換えられる外部入力**なので `parseLyricSheet` と同じ立場で `unknown` から入れる。形の決まりは `TapSession` のものであって保存先の都合ではないので、`tap-draft.ts` 側には置かない
- **保存先の名前はシートごとに分ける**（`lyric-stage:tap-draft:<sheet>`）。本編と `?lyrics=sample` が混ざると、**行数が合っているぶん気づかないまま別の曲の時刻を読み込む**
- **自動で再開する。**「下書きがあります → 再開」ボタンにすると、押し忘れて上から録り直し下書きを上書きするという、**塞ごうとしている事故が形を変えて残る**
- **破棄は 2 段階のボタン。`window.confirm()` は使わない。** この道具は `Space` と `Backspace` を横取りしているので、ネイティブのダイアログにキー操作の主導権が移ると、閉じた後にどちらが効いているのか読みにくい。身構えは打鍵・行のクリック・描き直しのいずれでも解ける
- **壊れた下書きをこちらから消さない。** 読めなくても道具は起動し、知らせを出して破棄ボタンを押せる状態にするに留める。手で直せば救えるかもしれない唯一の控えを、勝手に捨てない
  - **消さないだけでは足りず、自動保存も止める**（レビュー指摘 🔴）。止めないと**最初の 1 打鍵が上から書いて**、消さないと決めたはずの下書きが 1 行だけの新しい下書きに置き換わる。捨てるかどうかは破棄ボタン（＝人の意思）に委ね、捨てた時点で自動保存を戻す
- **保存の失敗は握り潰さない**（`DraftStore.save` は投げる）。容量や設定が理由なら次も失敗するので、一度失敗したら諦めて「閉じると収録は失われます」と出す。黙って続けると、**守られているつもりで守られていない**状態に気づけない
  - **その知らせは消えない**（レビュー指摘）。一度きりの知らせにすると次の描き直しで流れ、以降は何十行録っても警告なしで進む。案内の欄に必ず前置きし、`data-trouble` で色も変える。一度きりの知らせ（再開・破棄）とは別の変数に分ける
- **下書きにはシートの指紋（`sheetFingerprint`）を持たせる**（レビュー指摘）。保存先をシート名で分けるだけでは、**同じシートの歌詞が書き換えられた場合**を塞げない（1 行足して 1 行消せば行数も合う）。指紋は `title` と各行の `text` から取るので、**時刻を録り直しても無効にならない**。M6-3 はシートの中身を書き換えるので、その前に入れておく
- **下書きの状態機械は `tap-draft.ts` に置き、DOM から切り離す**（レビュー指摘）。上の 🔴 は、この判断が画面の組み立てに散っていて検査できなかったところから出た。`openDraft` / `keepDraft` / `discardDraft` / `noticeShown` が状態を返し、`mountTapTool` は映すだけにする。不具合はコンソールへ出したいが、それを純粋な側でやると検査のたびに雑音が出るので、状態に `error` を持たせて**遷移した側が書き出す**
- **破棄の身構えは書き出しでも解く**（レビュー指摘）。解除の経路が打鍵と行のクリックだけだと、「身構える → 書き出す → もう一度破棄を押す」で**身構えるつもりのクリックが即実行**になる。描き直し・打鍵・行のクリック・書き出しの全てで解く
- **保存先が無いときの置き場所（`unavailableDraftStore`）は、保存を求められたら投げる**（再レビュー指摘 🔴）。黙って受け取って捨てると `keepDraft` は成功として扱い、**画面には警告が出ないのに破棄ボタンだけが点灯し、閉じれば全部消える**。前の 🔴 と同じ「守られているつもりで守られていない」の別の入口だった。投げれば既存の失敗経路に合流し、最初の打鍵で画面に出る
- **`trouble` は「今この瞬間守られていないか」**（再レビュー指摘）。保存が通った時点で消す。持ち回すと、破棄に失敗した後などに**実際は保存できているのに警告だけが貼り付いて抜け出せない**。読めない下書きのとき（`saving: false`）は保存を試みないので消えない
- **`notice` / `trouble` は文面ではなく理由を持つ**（再レビュー指摘）。文面は `tap-view.ts`（`draftNoticeText` / `draftTroubleText`）が決める。domain の `OrderConflictError` が `problems` を持ち「何行目の何が」を表示側に委ねたのと同じ分け方。副産物として、版が上がっただけの下書きに「壊れています」と言わない文面へ直すのも 1 か所で済む
- **コンソールへ出すのは「前と違う不具合」のときだけ**（再レビュー指摘）。参照の違いで判定すると「遷移関数は必ず `error` を落とす」という規約に依存し、落とさない遷移（`noticeShown`）を配線に通せなくなる
- **「直近に何が起きたか」と「今も自動保存が止まっているか」は別の事実**（3 巡目レビュー指摘 🔴）。1 つの文面に畳んでいたため、読めない下書きを**破棄しそこねた瞬間**に `clear-failed` が `unreadable` を上書きし、**保存が止まっているという重い方の知らせだけが消えていた**（`saving` を戻す唯一の経路が破棄の成功なので、そこから抜けられなくなる）。文面は `trouble` と `saving` の両方から組み立てる
- **文面の対応表は型で網羅を締める**（3 巡目レビュー指摘）。`default` で素通しにすると、種類を増やしたとき**警告が空になる**か、新しい知らせが**別の文面で嘘をつく**。`never` に代入してコンパイルで止める
- **指紋の作り方を変えたら `DRAFT_VERSION` を上げる**（3 巡目レビュー指摘）。据え置くと、古い下書きが「歌詞が書き換えられました」と**誤った理由で**弾かれる
- 指紋の区切りは `\u0000`（NUL） にし、行数も混ぜる（再レビュー指摘）。改行を含む歌詞があると行の境目が曖昧になり、別のシートが同じ指紋になりうる
- **`mountTapTool` の DOM 配線には自動テストが無い**（再レビュー指摘、未対応）。jsdom を入れる話は下書きとは別の変更なので Issue #29 に切り出した。状態機械は純粋側で検査済みで、実ブラウザでも確認している
- `localStorage` は**触れた時点で投げうる**（保存を止めている設定）。`tap-main.ts` が try で包み、駄目なら何も覚えない `noDraftStore` を渡す。下書きが残らないだけで収録も書き出しもできる
- 目視の確認では、**この WSL の Vite dev サーバーは最初の `fetch` が返らないことがある**（接続待ちで固まる。別のリクエストを投げると解ける）。歌詞 JSON の読み込みが終わらず画面が「読み込み中…」で止まるが、アプリ側の不具合ではない

M6-3 の入り口（着手時に読む）:
- **本体は収録作業。**私（Claude）が書くのは「録れた JSON を投入して検査を通す」だけ。段取りは Issue #31
- 収録は **1 回で終わらせなくてよい**。M6-2b の下書きがあるので、閉じても開き直せば続きから録れる
- 投入後は `src/lyric-sheets.test.ts` の 4 つの検査（全行に effect / 同じ歌詞は同じ演出 / 縦書きにラテン文字なし / 最終行に duration）が通ることを確かめる。収録は `text` と `effect` を保つので普通は壊れないが、**壊れたら収録ではなくシートの取り違えを疑う**
- **実測と推定のズレの傾向を見る。**全体に一定方向へずれていたら叩き癖（人の反応の遅れ）なので、一括で補正するかを判断する。M6-1 で「オフセット欄は作らない／ズレは手で詰める」と決めているので、直すならここで
- 最後の行の `duration: 6.0` は M4-3 で置いた暫定値。**アウトロで `LaLaLa` がどこまで歌われているかは未確認**で、これを確定させるのが M6-3 の目的の一つ

M8（文字PV 化）の方針:
- **参考動画は【Aviutl】文字PV - ロウワー**（2026-08-22 に決定）。今の作りとの一番大きな差は**構図**で、中央に 1 行 1 サイズで置くのをやめ、画面の隅・斜め・大小の緩急で 1 行ずつ画を作る。合わせて図形グラフィック（帯・枠・英字サブテキスト）、ビート同期の衝撃、モノトーンの配色を入れる
- **背景の星空は文字PV 風に差し替える**（M8-2）。ただし `Starfield` を消すのではなく「描き手の 1 つ」に格下げし、`ScaledCanvas` / 音量解析 / `ReducedMotionQuery` の配線（M5 の資産）はそのまま使い回す
- 刻み順は 尺 → 構図 → 配色と背景 → 図形 → ビート同期。**構図が決まらないと背景の密度を決められない**ので、背景の差し替えは構図の後に置く

M8-0 で決めたこと:
- **尺をラスサビ 1 ブロック（`176.77`〜`203.82` / 27.05 秒）に絞る。** 文字PV は 1 行ずつ手で構図を作るので、51 行すべてに構図を用意するのは現実的でない。シートの構成上 `155.7`〜`179.78` が 24 秒の間奏、`203.82` で次のサビ頭に折り返すので、前後とも切れ目になっている。開始は歌（`179.78`）の 1 小節前（79.85 BPM / 1 小節 3.0055 秒）で、助走を付けて始まる
  - 20 秒に寄せたければ 7 行目を落として `197.81` 止め（21.0 秒）。`work.ts` の定数 1 個
  - **副産物として M6-3 の収録が 7 行で済む**
- **mp3 は加工しない。歌詞シートも 51 行のまま残す。** 加工版の設置は魔王魂の利用ルールで NG（「素材の扱い」）。シートを削らないのは、区間を広げ直せるようにするためと、M6-3 の収録が全曲基準の時刻で行われるため
- **「作品は 27 秒のもの」という見方にコード全体を揃える。** 区間の知識を再生コントロール・停止処理・初期位置に散らすと同じ判断が 3 か所に増える。秒数の読み替えを `WindowedPlayback`（`Playback` のデコレータ）に、歌詞側の付け替えを `sliceSheet`（domain の純粋関数）に集め、**区間を知っているのはこの 2 つだけ**にした。transport も背景も歌詞判定も、区間を知らないまま今のとおり動く
- **`Playback` に `pause()` を足した。** 終端で止めるのに `toggle()` を使うと、「今動いているか」を読んでから切り替えるまでの間に状態が変わりうるので、止めたつもりが再生の開始になる
- **終端の見張りは毎フレーム。`timeupdate` に任せない。** あのイベントは 250ms 程度の粗さでしか飛ばないので、終端を最大 0.25 秒行き過ぎてから止まる（作品の最後の 1 行が切れて見える）
- **終端で止まった後の `toggle()` は頭から流し直す。** これが無いと「再生 → 即座に見張りが止める」を繰り返し、一度最後まで聴いた人はリロードするまで二度と再生できない
- **読み込みが済むまで `duration` は 0 を名乗る。** 区間の長さは metadata が無くても分かるが、先に名乗ると再生コントロールが「長さが分かった」と判断してシークバーを開けてしまう。読み込み前の挙動を今までどおりに保つ
- **`currentTime` の上限は区間の長さで抑える（`this.duration` ではない）。** duration は読み込み前 0 なので、そちらで抑えると再生位置が 0 に貼り付いて見える
- **`sliceSheet` は「表示区間が重なる行」を残す。**「`time` が区間内」で選ぶと、区間の頭を跨いで出続けている行が消えて開幕だけ歌詞が抜ける。跨いだ行は区間の頭に置き直し、はみ出した `duration` は切り詰める
- 付け替えた時刻は 1ms の格子に丸める（`179.78 - 176.77 = 3.0100000000000193` のような埃を残さない）。収録ツールの格子は 10ms なので、実測値をここで粗くすることはない
- `tap.html`（収録ツール）には区間を適用しない。収録は全曲基準で行う
- **区間は本編シートに固有。`?lyrics=` で別のシートを見るときは曲を丸ごと流す**（レビュー指摘 🔴）。無条件に当てていたため、README が案内している `?lyrics=sample`（1〜33 秒のシート）が **1 行だけ残って 27 秒出っぱなし**になっていた。区間とシート名を `main.ts` で組にする
  - **切らない場合は `WHOLE_SONG`（`0`〜`Infinity`）を渡す。** 「区間があるとき／無いとき」で配線を分けない。`sliceSheet` は素通し、`WindowedPlayback` は素の `Playback` と同じ振る舞いになる（どちらも検査で守る）。分岐を置くと、**区間を使わない経路だけが検査もされないまま腐る**
- **`WorkWindow` は `domain/work-window.ts` に独立させる**（レビュー指摘）。歌詞（`sliceSheet`）と再生位置（`WindowedPlayback`）の両方が使う概念で、「歌詞の型と判定」のファイルの持ち物ではない
- **区間の不変条件（`0 <= start < end`）は `WindowedPlayback` の組み立て時に投げる**（レビュー指摘）。書き間違えると `duration` が 0 に潰れて**シークバーが無効のまま**という、原因の分かりにくい壊れ方をする
- **既に読み込みが済んだ `Playback` を包む場合も頭出しする**（レビュー指摘）。購読のコールバックの中だけで頭出しすると、その場合に一度も呼ばれず作品が曲の 0 秒から始まる。composition root の**生成順への暗黙の依存**になっていた
- **区間の手前へ落ちた時も連れ戻す**（レビュー指摘）。音源が区間より短いと終端の見張りが一度も働かないまま曲が自然に終わり、次の再生は要素の仕様で 0 秒から始まる（曲が丸ごと流れ出す）
- **終端の見張りは `timeupdate` 経由でも掛ける**（レビュー指摘）。`keepInWindow()` は `Playback` port に無いので、`main.ts` の配線を落としても型検査もテストも通り、**作品だけが終端で止まらなくなる**。毎フレームの駆動が本命で精度もそちらだが、保険が無いと落ちた時の被害が「曲の残り全部が流れる」になる
- **本編シート × `WORK_WINDOW` の噛み合わせを `lyric-sheets.test.ts` で見張る**（レビュー指摘）。M6-3 で 51 行の time を実測に差し替えると歌が動くので、区間だけ据え置くと**全テスト緑のまま作品が空になる**
- **丸めて 0 になるほどしか残らない行は、行ごと落とす**（レビュー指摘）。`duration` だけ落とすと「次の行まで表示」に化けて、元より長く出ることになる
- **どのシートにどの区間を当てるかは `work.ts` の `workWindowFor` が持つ**（再レビュー指摘）。`main.ts` に三項演算子で書くと**テストが届かず、書き戻しても全テストが緑のまま公開ページだけが壊れる**。上の 🔴 がまさにその形だったので、直したものと同じ判断をテストで固定する
- **見張りの中の `pause()` / `seek()` は、それ自体が見張りを呼び返す**（再レビュー指摘）。`timeupdate` の保険を入れたことで再入経路ができた。収束の根拠は経路ごとに違う:
  - 上限側 — `pause()` が必ず状態を変えるので、次の呼び出しは冒頭で返る
  - 頭出し — `positioned = true` を `seek()` の**前**に立てている
  - 下限側 — **収束の保証が無かった**。`seek` は音源の長さで頭打ちになるので、区間の頭に届かない音源（取り違えた mp3）では戻しても届かず連鎖が終わらない。`duration > start` を条件に足して塞いだ
  - 検査には**イベントを起こす偽物**（`EmittingSource`）を別に用意する。素の偽物は `seek` / `pause` が黙っているので、この再入は一度も通っていなかった
- `WHOLE_SONG` の「素通し」は厳密には恒等ではない（`sliceSheet` の 1ms 丸めが掛かる／読み込み時に `seek(0)` が入る）。収録の格子は 10ms なので実害は無い

注意点（レビュー指摘）:
- JS 内の `fetch('/audio/x.mp3')` は Vite が書き換えないので Pages で 404 になる。`import.meta.env.BASE_URL` を前置するユーティリティを M1 で用意する
- 行を出し入れするたびに SplitText インスタンスの `revert()` を呼ばないと DOM ノードが増え続ける。ライフサイクル管理は stage 層に持たせる
- クレジット表記を HTML と JSON のどちらを正にするか M2 で決める（現状は HTML にハードコード）

## データ構造（M2 で確定した）

```jsonc
{
  "title": "シートの名前",
  "lines": [
    { "time": 12.4, "text": "ここに一行", "effect": "typewriter", "duration": 3.2 }
  ]
}
```

`time` は曲の先頭からの秒数。`effect` は演出プリセット名。`duration` を省いたら次の行の `time` まで表示。

検討時にあった `credit` と `audio` は持たせないことにした。クレジットは HTML が正、音源パスは
`src/work.ts` が正で、シートごとに変える必要が無いため。
